// Single source of truth for the site's absolute base URL — used by
// sitemap.ts, robots.ts, layout.tsx (metadataBase), and every page's
// JSON-LD `url` field so canonical/OG/sitemap URLs never drift apart.
export const SITE_URL = "https://akshaastracomics.thoughtleadersconsulting.co.in";
